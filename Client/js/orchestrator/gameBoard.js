define(
    'client.orchestrator.gameBoard',
    [
    ],
    function () {


        function GameBoard(boardWidth, boardHeight) {

        }

        GameBoard.prototype.init = function () {
        };

        GameBoard.prototype.render = function () {
        };

        GameBoard.prototype.tick = function () {
        };

        return GameBoard;
    });

